name: termination
model:
  embed_dim: 32
  residual_connections: true
  agg_fn: MAX
  num_mp_layers: 2
  dropout: 0.1
  termination_mode: distance
  termination_distance_latent: processed
  termination_distance: l2
  termination_distance_threshold: 5
  termination_distance_signal: false
training:
  epochs: 500
  learning_rate: 1.0e-05
  max_grad_norm: 1.0
  batch_size: 10
  eval_interval: 10
  seed: 42
data:
  train_path: data/train_dataset.npz
  val_path: data/val_dataset.npz
  test_path: data/dataset_20g_200n.npz
logging:
  use_wandb: true
  wandb_project: nge-exp
  wandb_entity: ''
  log_interval: 1
  save_checkpoints: true
  checkpoint_interval: 50
